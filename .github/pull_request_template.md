<!-- A concise PR. The title follows Conventional Commits (see CONTRIBUTING.md). -->

## Summary

<!-- What does this change do and why? -->

## Checklist

- [ ] Title follows Conventional Commits (`type(scope): subject`)
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] `npm test` passes
- [ ] `npm run build` run and the updated `dist/` is committed (if `src/` changed)
- [ ] Docs (`README.md`) updated if inputs/outputs or behavior changed
